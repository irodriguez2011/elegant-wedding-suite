Remove the `.olive` sprig SVG (and its wrapper `<svg class="olive">…</svg>`) that sits directly above the "IN LIEU OF GIFTS" eyebrow in the Honeymoon Fund section of `public/wedding.html`. Leave all other elements — eyebrow, divider, heading, heart, paragraph, button, layout, spacing, colors — untouched. The unused `.honeymoon .olive` CSS rule can stay (harmless) to keep the diff minimal.
