Update Isamar's bridesmaid card `<img>` in `public/wedding.html` — change `object-position: center 60%` to `object-position: 35% 60%` to shift the photo ~15% to the right while keeping the vertical crop.