Update the Honeymoon Fund section in `public/wedding.html` to match the reference:

1. **Stack the title**: Change "Honeymoon Fund" to render on two lines with "Honeymoon" on top and "Fund" underneath (using a `<br>` inside the `.section-title`).
2. **Refine the "IN LIEU OF GIFTS" ornament**: Rebuild the SVG above the tag to match the reference — two thin gold hairlines flanking a small centered diamond/sparkle, sitting just below the "IN LIEU OF GIFTS" text (not above). Move the sprig to sit above the text and simplify it to a single delicate tilted leafy branch.
3. **Keep** the centered gold heart under the title, ivory background, dark walnut title color, and blue Contribute button — those already match.

No changes to layout, photo, or other sections.